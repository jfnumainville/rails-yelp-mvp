<h1>Restaurants</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li> Name: <%= link_to restaurant.name, restaurant_path(restaurant) %> Category: <%= restaurant.category %></li>
  <% end %>
</ul>
